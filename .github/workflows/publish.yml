name: Publish to PyPI

on:
  push:
    tags: ["v*"]

# v0.6.1 H7: scoped permissions + concurrency lock.
# v0.6.3 I6: OIDC trusted publishing ג€” no long-lived token in CI.
permissions:
  contents: read
  id-token: write  # required for OIDC trusted publishing to PyPI

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    # v0.6.3 I6: GitHub Environment for extra protection on the release path.
    # Configure at: Settings ג†’ Environments ג†’ New environment "pypi"
    environment:
      name: pypi
      url: https://pypi.org/p/cloakllm-mcp

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build

      - name: Build package
        run: python -m build

      # v0.6.3 I6: OIDC trusted publishing replaces PYPI_API_TOKEN.
      # Prerequisite (one-time): configure trusted publisher at
      #   https://pypi.org/manage/project/cloakllm-mcp/settings/publishing/
      # with:
      #   Owner:        cloakllm
      #   Repository:   cloakllm-mcp
      #   Workflow:     publish.yml
      #   Environment:  pypi
      # See: https://docs.pypi.org/trusted-publishers/
      - name: Publish to PyPI (OIDC)
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
        with:
          skip-existing: true
          print-hash: true
